name: Setup Tools
description: Setup required tools in the workspace

runs:
  using: composite
  steps:
  - name: Setup Tools
    uses: egose/actions/asdf-tools@c90a739e44e3f1fe6ccdc2581bd908db3c302ee4

  - name: Install python tools
    run: |
      pip install -r requirements.txt
      asdf reshim
    shell: bash
