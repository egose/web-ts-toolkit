name: Setup Tools
description: Setup required tools in the workspace

runs:
  using: composite
  steps:
  - name: Setup Tools
    uses: egose/actions/asdf-tools@dfe5d14c69e654ca87d9f6fc491084a59eed6181

  - name: Install python tools
    run: |
      pip install -r requirements.txt
      asdf reshim
    shell: bash
