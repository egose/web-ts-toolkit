name: Setup Tools
description: Setup required tools in the workspace

runs:
  using: composite
  steps:
  - name: Setup Tools
    uses: egose/actions/asdf-tools@582b6748271fe29043a0b8883c14d18184a6570a

  - name: Install python tools
    run: |
      pip install -r requirements.txt
      asdf reshim
    shell: bash
